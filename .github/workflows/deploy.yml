name: Deployment

on:
  push:
    branches: [ "master", "main"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Le déploiement ne se lance que si les tests (job laravel-tests du workflow CI) passent
    # Note: Pour une dépendance stricte entre fichiers, on utilise workflow_run.
    # Ici, on part du principe que ce workflow est autonome ou déclenché manuellement/après merge.
    steps:
    - name: Notify Webhook - Deployment Started
      run: |
        curl -X POST "https://channelmarket.net/webhook/deploy" \
          -H "Content-Type: application/json" \
          -H "X-Deploy-Token: faa82cf54d512bf07b72e29d3501ff24" \
          -d '{"commit_sha":"${{ github.sha }}", "commit_message":"${{ github.event.head_commit.message }}", "status":"deploying"}' || true

    - name: Diagnostic réseau (Vérification Connectivité)
      run: |
        echo "Tentative de ping vers l'hôte..."
        ping -c 3 ${{ secrets.VPS_HOST }} || echo "Le serveur ne répond pas au ping (normal si ICMP est bloqué)"
        echo "Vérification de l'ouverture du port SSH..."
        nc -zv -w 5 ${{ secrets.VPS_HOST }} ${{ secrets.VPS_SSH_PORT || 22 }} || echo "ERREUR : Le port SSH est FERMÉ ou INACCESSIBLE"

    - name: Deploy to VPS via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USERNAME }}
        key: ${{ secrets.VPS_SSH_KEY }}
        port: ${{ secrets.VPS_SSH_PORT || 22 }}
        timeout: 60s
        debug: true
        script: |
          # S'assurer que le dossier existe et s'y déplacer
          mkdir -p ${{ secrets.VPS_PATH }}
          cd ${{ secrets.VPS_PATH }}
          
          # Sauvegarder le commit actuel pour un éventuel rollback
          PREVIOUS_COMMIT=$(git rev-parse HEAD 2>/dev/null || echo "")
          echo "Dernier commit stable : $PREVIOUS_COMMIT"

          # Mise à jour du code dynamique par rapport à la branche active
          if [ ! -d ".git" ]; then
            echo "Premier déploiement : Clonage du projet..."
            git clone https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git .
            git remote set-url origin https://github.com/${{ github.repository }}.git
          else
            echo "Mise à jour du code existant pour la branche ${{ github.ref_name }}..."
            git fetch origin
            git reset --hard origin/${{ github.ref_name }}
          fi
          
          # Nettoyage et arrêt propre pour éviter les conflits de conteneurs orphelins
          echo "Arrêt propre des conteneurs existants et nettoyage des conflits..."
          docker compose down --remove-orphans || true
          docker container prune -f
 
          echo "Lancement de Docker Compose avec construction..."
          if ! docker compose up -d --build; then
            echo "ERREUR : Échec du build Docker. Rollback vers $PREVIOUS_COMMIT..."
            if [ -n "$PREVIOUS_COMMIT" ]; then
              git reset --hard $PREVIOUS_COMMIT
              docker compose up -d --build
            fi
            exit 1
          fi
          
          # Extraire les assets compilés (CSS/JS) depuis le conteneur vers l'hôte pour Nginx
          echo "Extraction des assets compilés (CSS/JS)..."
          docker cp channel-market-app-1:/var/www/public/build ./public/
          chmod -R 755 public/build
 
          # Fix Permissions & Folders
          docker compose exec -T --user root app mkdir -p storage/framework/views storage/framework/cache storage/framework/sessions bootstrap/cache
          docker compose exec -T --user root app chmod -R 777 storage bootstrap/cache
          docker compose exec -T --user root app chown -R www-data:www-data storage bootstrap/cache
          
          # Optimisations Laravel
          docker compose exec -T app php artisan storage:link --force
          docker compose exec -T app php artisan migrate --force
          docker compose exec -T app php artisan config:cache
          docker compose exec -T app php artisan route:cache
          docker compose exec -T app php artisan view:cache
          
          # Nettoyage des images inutilisées
          docker image prune -f
          
          # --- VERIFICATION DE SANTE (Healthcheck) ---
          echo "Attente de la stabilisation des conteneurs (20s)..."
          sleep 20
          
          echo "Vérification de l'URL : https://channelmarket.net"
          # On vérifie si le site répond un code 200 ou 302 (OK)
          HTTP_STATUS=$(curl -o /dev/null -s -w "%{http_code}" https://channelmarket.net)
          
          if [ "$HTTP_STATUS" -eq 200 ] || [ "$HTTP_STATUS" -eq 302 ]; then
            echo "✅ Succès ! Le site répond avec le code $HTTP_STATUS."
          else
            echo "🚨 ERREUR CRITIQUE : Le site répond $HTTP_STATUS (404/500). Lancement du Rollback..."
            if [ -n "$PREVIOUS_COMMIT" ]; then
              git reset --hard $PREVIOUS_COMMIT
              docker compose up -d --build
              echo "Rollback effectué vers le commit $PREVIOUS_COMMIT."
            fi
            exit 1
          fi
 
    - name: Notification Succès (Email & Webhook)
      if: success()
      run: |
        curl -X POST "https://channelmarket.net/webhook/deploy" \
          -H "Content-Type: application/json" \
          -H "X-Deploy-Token: faa82cf54d512bf07b72e29d3501ff24" \
          -d '{"commit_sha":"${{ github.sha }}", "status":"success"}' || true
 
        curl -X POST https://api.resend.com/emails \
          -H "Authorization: Bearer ${{ secrets.RESEND_API_KEY }}" \
          -H "Content-Type: application/json" \
          -d '{
            "from": "noreply@channelmarket.net",
            "to": ["elfridayemadje5@gmail.com", "digitaleflex@gmail.com"],
            "subject": "✅ Déploiement RÉUSSI - Channel Market",
            "html": "<h3>Félicitations !</h3><p>Le déploiement de <b>Channel Market</b> sur ton VPS s est déroulé avec succès.</p><p>Version : ${{ github.sha }}</p><p><a href=\"https://channelmarket.net\">Accéder au site</a></p>"
          }'
 
    - name: Notification Échec (Email & Webhook)
      if: failure()
      run: |
        curl -X POST "https://channelmarket.net/webhook/deploy" \
          -H "Content-Type: application/json" \
          -H "X-Deploy-Token: faa82cf54d512bf07b72e29d3501ff24" \
          -d '{"commit_sha":"${{ github.sha }}", "status":"failed"}' || true
 
        curl -X POST https://api.resend.com/emails \
          -H "Authorization: Bearer ${{ secrets.RESEND_API_KEY }}" \
          -H "Content-Type: application/json" \
          -d '{
            "from": "noreply@channelmarket.net",
            "to": ["elfridayemadje5@gmail.com", "digitaleflex@gmail.com"],
            "subject": "🚨 ERREUR de Déploiement - Channel Market",
            "html": "<h3>Attention !</h3><p>Le déploiement de <b>Channel Market</b> a échoué.</p><p>Veuillez vérifier les logs sur GitHub Actions immédiatement.</p><p><a href=\"${{ github.server_url }}/${{ github.repository }}/actions\">Voir les erreurs</a></p>"
          }'
